# assets/

Art and audio. **No scripts.**

- `third_party/` — assets downloaded from elsewhere (packs, marketplace, CC art).
- Everything else in `assets/` is **first-party** by convention (art and audio we authored).

Reference assets from scenes via `res://assets/...`.
