# sandbox/

Playground for developing and testing new features. Throwaway by default.

Whatever pans out gets promoted into `src/` (or extracted into a `systems/` component if it turns out to be reusable and game-agnostic). **Never shipped.**

## Dependencies

May depend on anything. **Nothing** may depend on `sandbox/`.
